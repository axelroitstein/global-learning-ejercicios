// Se exporta la función flecha para ser utilizada en otros archivos

export const sum = (a, b) => a + b

export const substract = (a, b) => a - b

export const multiply = (a, b) => a * b

export const divide = (a, b) => a / b

/* Alternativa de exportación:
Exporta por defecto todo lo que se incluya. Acá se exporta todo y en la forma anterior sólo se exporta lo que se necesita
export default {

    sum,
    substract,
    multiply
}

*/
